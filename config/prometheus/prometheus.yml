global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'realtime-gateway'
    metrics_path: '/realtime-gateway/actuator/prometheus'
    static_configs:
      - targets: ['realtimegateway-app:8080']
    relabel_configs:
      - source_labels: [__address__]
        target_label: instance
        replacement: 'realtime-gateway'
